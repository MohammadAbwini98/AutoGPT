// Chat config: select conversation by href and static query

export const CHAT_CONFIG = {
  CONVERSATION_HREF: "/c/6994c86e-f0f4-832e-80f0-1d2d5343d12a",
  QUERY: "Summarize the key points we discussed so far in bullet points.",
  RESPONSE_TIMEOUT_MS: 120000,
  LOGIN_TIMEOUT_MS: 90000,
  HEADLESS: true,
  CHROME_PROFILE_DIR: "./chrome-profile"
};
